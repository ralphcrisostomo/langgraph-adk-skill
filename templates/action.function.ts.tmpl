// If this action renders Ink (rename this file to .tsx), make the FIRST line:
//   /** @jsxImportSource react */
// so it transpiles with React JSX when run as a global bin from any directory
// (Bun resolves jsxImportSource from the launch cwd's tsconfig otherwise).
import type { Action } from './_types';
import { withSpinner } from '../ui';
import { printContextBar } from '../session-core';

export const {{ACTION_CAMEL}}: Action = {
  name: '{{ACTION_NAME}}',
  description: '{{ACTION_DESCRIPTION}}',
  params: [
    // {{PARAM_DEFS}}  e.g. { name: 'text', message: 'Text to process', type: 'input', required: true }
  ],
  run: async (values, ctx) => {
    // {{FUNCTION_BODY}} — plain async logic.
    // ctx.llm.invoke(...) for a single model call; wrap slow work in
    // withSpinner('working…', () => ...) for an Ink spinner; ctx.log (chalk) to print.
    //
    // MANDATORY if this action calls the LLM: surface the model id + context usage after the call —
    //   printContextBar(ctx.log, ctx.contextWindow, [...messages, { role: 'assistant', content: reply }], ctx.model);
    // (Remove the import above if this action does NOT use the model.)
    //
    // For an interactive multi-turn loop, rename to .tsx and render
    // <SessionApp respond={...}/> from '../ink/SessionApp' (it shows the bar live).
  },
};
