import type { Action } from './_types';
import { withSpinner } from '../ui';

export const {{ACTION_CAMEL}}: Action = {
  name: '{{ACTION_NAME}}',
  description: '{{ACTION_DESCRIPTION}}',
  params: [
    // {{PARAM_DEFS}}  e.g. { name: 'text', message: 'Text to process', type: 'input', required: true }
  ],
  run: async (values, ctx) => {
    // {{FUNCTION_BODY}} — plain async logic.
    // ctx.llm.invoke(...) for a single model call; wrap slow work in
    // withSpinner('working…', () => ...) for an Ink spinner; ctx.log (chalk) to print.
    // For an interactive multi-turn loop, rename to .tsx and render
    // <SessionApp respond={...}/> from '../ink/SessionApp'.
  },
};
